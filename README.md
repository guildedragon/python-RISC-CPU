# what the project is:
this project is to be able to expand upon my knowledge of CPU's and programming in order to create a simulated CPU in python

# the goals of the project are:
    *   build a CPU architecture from scratch
    *   use a clear Fetch-Decode_execute cycle
    *   nake it simple enough to implement and understand
    *   make the ISA realistic enough to puch CPU architecture knowledge
    *   make it possible to run real assembly programs with loops, functions, branches, and memory access.

# the main features are:

# how to run it:

# example usage:

